create database niec;

create table user(username varchar(25) unique not null, email varchar(255) character set latin1 collate latin1_swedish_ci unique not null, password varchar(255), bio text, dp varchar(255), created_at datetime);

create table article( id bigint primary key, created_at datetime, text longtext);