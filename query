create database niec;

create table user(id bigint primary key auto_increment, username varchar(25) unique not null, email varchar(255) character set latin1 collate latin1_swedish_ci unique not null, password varchar(255), bio text, dp varchar(255), created_at datetime);

create table article( id bigint primary key auto_increment, title varchar(255), created_at datetime, text longtext);